# Runtime dependencies for the deployed API server ONLY. Deliberately does
# NOT include torch/transformers -- the live app never imports
# context_classifier.py or emotion.py (confirmed: nothing under app/api/
# imports them). Those two only run inside scripts/seed_db.py, which you run
# once, locally, to regenerate the bulk synthetic dataset -- see
# requirements-datagen.txt. Keeping them out of this file is what makes the
# deployed server small and fast to build (torch alone is 500MB-2GB and
# would blow most free-tier build limits).
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic>=2.9  # google-genai pulls in a newer 2.x; a tight pin here just fights it
python-dotenv==1.0.1

# Gemini API (free tier) for the live demo chat (see services/gemini_client.py,
# api/chat.py). Needs GEMINI_API_KEY in .env / host env vars.
google-genai>=1.0

# Claude API -- optional, only for the on-demand profile explanation
# (see services/claude_client.py). Needs ANTHROPIC_API_KEY in .env / host env vars.
anthropic>=0.68
